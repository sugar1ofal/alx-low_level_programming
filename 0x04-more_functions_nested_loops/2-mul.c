#include "main.h"

/**
 * mul - multiplies two integers or numbers
 * @a: integer to be multiplied to b
 * @b: integer to be multiplied to a
 * Return: the result of the multiplication operation
 */
int mul(int a, int b)
{
	return (a * b);
}
